import React from"react";import{Link,useParams}from"react-router-dom";import{rooms}from"../components/catalog/catalogData";import"./FurniturePages.css";const RoomsPage=()=>{const{room}=useParams(),active=room?rooms.find(r=>r.slug===room):null;if(active)return <div><section className="collection-hero"style={{backgroundImage:`linear-gradient(90deg,rgba(20,16,12,.68),rgba(20,16,12,.1)),url(${active.image})`}}><div className="collection-hero-content"><p className="j-kicker"style={{color:"#eee5da"}}>Shop the room</p><h1 className="j-title">{active.name}</h1><p>{active.copy}</p><Link to={`/shop?category=${encodeURIComponent(active.name)}`}className="j-button light">Shop this room</Link></div></section><section className="j-section"><div className="j-container"><div className="section-head"><h2 className="j-title j-title-lg">Pieces for {active.name}</h2><Link to="/shop"className="j-link">View all</Link></div><div className="room-placeholder"><p>Build your {active.name.toLowerCase()} around pieces selected for comfort, balance and character.</p><Link to={`/shop?category=${encodeURIComponent(active.name)}`}className="j-button">Browse furniture ↗</Link></div></div></section></div>;return <div><section className="shop-hero"><div className="shop-hero-inner"><div><p className="j-kicker">Interior edit</p><h1 className="j-title">Shop the room.</h1></div><p className="j-copy">Start with the atmosphere you want, then build your space around pieces that belong together.</p></div></section><section className="j-section"><div className="j-container rooms-page-grid">{rooms.map(r=><Link className="room-feature"to={`/rooms/${r.slug}`}key={r.slug}><img src={r.image}alt={r.name}/><div className="room-feature-content"><h2>{r.name}</h2><p>{r.copy}</p><span className="j-link">Explore room ↗</span></div></Link>)}</div></section></div>};export default RoomsPage;
